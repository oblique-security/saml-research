{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SAML validation/assertion surface site",
  "description": "A single line within a file that represents an important step in SAML validation",
  "type": "object",
  "required": ["id", "description", "location"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable per-run id, e.g. 's-0003'"
    },
    "description": {
      "type": "string",
      "description": "A short description of what this line does, similar to 'entrypoint to response validation...', 'performs signature validation...', 'reads user email attribute...', 'checks for element reanchoring...'"
    },
    "location": {
      "type": "string",
      "description": "file:line (or file:line-line) of the code implementing this site."
    }
  }
}
